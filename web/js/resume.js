class Resume{static#build(data){const content=document.getElementById("content");const table=document.createElement("table");data.forEach(category=>{if(category.title){const tr=document.createElement("tr");const th=document.createElement("td");th.colSpan=2;th.classList.add("category-title");Object.keys(category.title).forEach(local=>{const span=document.createElement("span");span.classList.add("localized");span.lang=local;span.innerHTML=category.title[local];th.appendChild(span);});tr.appendChild(th);table.appendChild(tr);}
if(category.timeline){category.timeline.forEach(group=>{const tbody=document.createElement("tbody");group.forEach(item=>{const tr=document.createElement("tr");{const td=document.createElement("td");td.classList.add("nowrap","right");td.innerHTML=item.time;tr.appendChild(td);}
{const td=document.createElement("td");const container=document.createElement("div");container.classList.add("container");if(item.highlight){td.classList.add("framed");container.classList.add("framed");}
Object.keys(item.item).forEach(local=>{const span=document.createElement("span");span.classList.add("localized");span.lang=local;if(item.href!==null){span.innerHTML=item.item[local];span.classList.add("link");const a=document.createElement("a");a.href=item.href;a.target="_blank";a.appendChild(span);const icon=document.createElement("span");icon.classList.add("open","localized","nowrap");icon.lang=local;{const span=document.createElement("span");span.classList.add("localized");span.lang="en";span.innerHTML="[open]";icon.appendChild(span);}
{const span=document.createElement("span");span.classList.add("localized");span.lang="de";span.innerHTML="[öffnen]";icon.appendChild(span);}
a.appendChild(icon);container.appendChild(a);}else{span.innerHTML=item.item[local];container.appendChild(span);}});td.appendChild(container);tr.appendChild(td);}
tbody.appendChild(tr);});table.appendChild(tbody);});}});content.appendChild(table);}
static build(){fetch("json/resume.json").then(response=>response.json()).then(data=>{Resume.#build(data.content);}).then(()=>{switch_lang(window.lang);}).catch(console.error);}}